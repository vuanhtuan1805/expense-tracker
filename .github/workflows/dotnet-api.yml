name: .NET Core Web API CI/CD

on:
  push:
    branches:
      - main
      - develop
      
env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}/expense-tracker-api
      
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - run: dotnet restore
      - run: dotnet build --no-restore --configuration Release
      - run: dotnet test --no-build --configuration Release --verbosity normal
      - run: dotnet publish -c Release -o ./publish
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          token: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          TAG=$(if [ "${GITHUB_REF##*/}" == "main" ]; then echo "latest"; else echo "dev"; fi)
          echo "Using tag: $TAG"
          docker build -t $IMAGE_NAME:$TAG .
          docker push $IMAGE_NAME:$TAG


#   docker-build-push:
#     needs: build-test
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}
#       - name: Build and push Docker image
#         uses: docker/build-push-action@v5
#         with:
#           push: true
#           tags: your-docker-user/expense-tracker-api:${{ github.ref_name }}

  cd:
    needs: ci
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
        - name: Login into GCHR
          uses: docker/login-action@v3
          with:
            registry: ghcr.io
            username: ${{ github.actor }}
            password: ${{ secrets.GITHUB_TOKEN }}

        - name: Pull Docker image
          run: docker pull $IMAGE_NAME:latest
        
        - name: Deploy to Azure
          uses: azure/webapps-deploy@v2
          with:
                app-name: 'ExpenseTrackerAPI'
                slot-name: 'production'
                images: $IMAGE_NAME:latest
